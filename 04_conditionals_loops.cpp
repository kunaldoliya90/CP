#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*

    int a = 2;
    int b = 3;
    cout<<(a <= b);
    cout<<(a >= b);
    cout<<(a == b);
    cout<<(a != b);
    // This will give answer in true or false

    */

    /*

    To check multiple condition we use &&, ||
    && - For checking both true compulsory
    || - for checking one true among two.

    */

    /*

    int a = 2;
    int b = 3;
    int c = 1;
    cout << ((b > a) || (b > c));

    */

    // If Condition - For checking a certain condition
    /*
    int n;
    cin >> n;
    if ((n % 2) == 0)
    {
        cout << "Yes";
    }
    else
    {
        cout << "No";
    }
    */

    /*

    // Scope - If we are using any new variable inside
    // any if condition then that variable doesn't exist for main fun
    // Local variable...
    int n;
    cin >> n;
    if ((n % 2) == 0)
    {
        int x = 2;
        cout << "Yes";
    }
    cout << x;

    */

    // Loops - While Loops
    /*
    Syntax - while(condition){//Code}
    for(initialisation; condition; operations){//Code}
    Nested Loops - using Loop inside a loop
    */
    /*
     int i = 1;
     while (i <= 10)
     {
         cout << i << " ";
         i++;
     }
     cout << endl;
     for (int j = 1; j <= 10; j++)
     {
         cout << j << " ";
     }

     int n;
     cin >> n;

     for (int i = 1; i <= n; i++)
     {
         for (int j = 1; j <= i; j++)
         {
             cout << "*";
         }
         cout << endl;
     }
     */

    // Break & Continue
    
}